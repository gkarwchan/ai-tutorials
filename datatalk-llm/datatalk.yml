name: datatalk
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - jupyterlab
  - ipywidgets
  - pip
  - pip:
      - openai
      - python-dotenv
      - tqdm
      - elasticsearch
      - jupyterlab-widgets

